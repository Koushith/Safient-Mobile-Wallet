import {ImageSourcePropType} from 'react-native';
import {Networks} from '@stackupfinance/config';
import {PolygonLogo} from '.';

export const NetworksLogos: Record<Networks, ImageSourcePropType> = {
  Polygon: PolygonLogo,
};
